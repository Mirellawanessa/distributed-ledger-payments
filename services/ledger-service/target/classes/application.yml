spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/ledger
    username: ${DB_USERNAME:ledger}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
